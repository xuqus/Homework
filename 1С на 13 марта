МояСтрока = "I love pancakes.";
Сообщить(МояСтрока);     

				//ковычки
Текст = """Ковычки"" пишутся так"; // ""*текст*""
Сообщить(Текст);

    			//длина строки
Сообщить(СтрДлина(МояСтрока)); //длина строки  

                //удаление пробелов
СтрокаСОтступами = "       Привет!   ";
Сообщить(СокрЛ(СтрокаСОтступами));
Сообщить(СокрП(СтрокаСОтступами));
Сообщить(СокрЛП(СтрокаСОтступами));  

                //извлечение части строки
КусокСтроки=Лев(МояСтрока,6);
Сообщить(КусокСтроки);
ВторойКусокСтроки = Прав(МояСтрока, 9);
Сообщить(ВторойКусокСтроки);
Сообщить(Сред(МояСтрока,3,13));  //начало (3), длина (13)

                  //Поиск подстроки
НоваяСтрока = "У меня было 10 яблок и 3 апельсина";
Поиск = СтрНайти(НоваяСтрока, "апельсин");
Сообщить(Поиск);  //слово "апельсин" начинается с 26 символа

                    //Замена текста
Текст = "Сегодня хорошая погода";
НовыйТекст = СтрЗаменить(Текст, "хорошая", "отличная");
Сообщить(НовыйТекст); //"Сегодня отличная погода"

                     //Верхний и нижний регистры
Сообщить(ВРег(Текст));                
Сообщить(НРег(Текст));                          

					//Разделение строки на массив и цикл
МояСтрока="яблоки, бананы, помидоры";
МойМассив=СтрРазделить(МояСтрока,",");
Для каждого ФруктыОвощи из МойМассив Цикл
	Сообщить(СокрЛП(ФруктыОвощи));
КонецЦикла;

						//проверка на пустую строку
Строка_1="";
Если ПустаяСтрока(Строка_1) Тогда
	Сообщить("Строка_1 пустая!");
КонецЕсли;

                       	//строка не пустая + вывод кол-ва символов
Если не ПустаяСтрока(Текст) Тогда
	Сообщить("Строка ""Текст"" не пустая, её длина: " +СтрДлина(Текст)); 
КонецЕсли;
						// сшиваем строки 
Часть_1="АБВГД"; 
Часть_2="ЕЁЖЗИ";  
ВсёВместе=Часть_1 + Часть_2;
Сообщить(ВсёВместе);
